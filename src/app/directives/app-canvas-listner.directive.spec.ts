import { AppCanvasListnerDirective } from './app-canvas-listner.directive';

describe('AppCanvasListnerDirective', () => {
  it('should create an instance', () => {
    const directive = new AppCanvasListnerDirective();
    expect(directive).toBeTruthy();
  });
});
